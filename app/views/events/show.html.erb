<div class="container" style="margin-top: 200px;">

  <div class="row" style="border: 1px solid red">
    <div class="col-lg-2">
      <%= attachment_image_tag @event, :event_image, :fill, 100, 100, format: 'jpeg', fallback: "/images/no_image.jpg" %>
    </div>
    <div class="col-lg-6">
     <%= @event.event_title %>
    </div>
    <div class="col-lg-4">
     <div class="row">
       <div class="col-lg-12">
         <% if @event.favorited_by?(current_user) %>
           <div>
             <%= link_to event_asiatos_path(@event), method: :delete do %>
               <i class="fa fa-heart" aria-hidden="true" style="color: red;"></i>
               <%= @event.asiatos.count %> いいね
             <% end %>
         </div>
       <% else %>
         <div>
           <%= link_to event_asiatos_path(@event), method: :post do %>
             <i class="fa fa-heart-o" aria-hidden="true"></i>
             <%= @event.asiatos.count %> いいね
           <% end %>
         </div>
        <% end %>
       </div>
     </div>
     <div class="row">
       <div class="col-lg-12">
         <%= @event.event_day%>
       </div>
     </div>
    </div>
  </div>

  <div class="row">

    <div class="col-lg-8" style="border: 1px solid blue;">
      <h1>出演アーティスト一覧</h1>
      <% @event.performers.each do |f| %>
  			<div>

  				<div>
  					<h3><%= f.artist.artist_name %></h3>

  				</div>
  			</div>

  	<% end %>
    </div>

    <div class="col-lg-4">
      <% if current_user.admin %>
      <div class="row">
        <div class="col-lg-6">
          <%= link_to "編集", edit_event_path%>
        </div>
        <div class="col-lg-6">
          <%=link_to "削除", event_path(@event), method: :delete, "data-confirm" => "本当に削除しますか？", method: :delete %>
        </div>
      </div>
      <% end %>
      <div class="row">
        <div class="col-12">
          <h5>アクセス情報</h5>
          <h5>住所</h5>
          <%= @event.event_address %>
          <!-- 地図 -->
        </div>
      </div>
    </div>

  </div>

  <div class="col-12">
    <h1>コメント記入</h1>
    <%= form_for [@event, @comment] do |f| %>
      <div class="row">
        <div class="col-sm-12">
          <%= f.text_area :comment, rows:'5', class: "form-control",placeholder: "コメントをここに" %>
        </div>
      </div>
      <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
    <% end %>
    <form>

    </form>
  </div>

  <div class="col-12">
    <h1>コメント一覧</h1>
    <each>

    </each>


  </div>
</div>
