<div class="container" style="margin-top: 200px;">
  <div class="row">
    <div class="col-lg-4" style="border: 1px solid red;">
     <%= attachment_image_tag @user, :user_image, :fill, 100, 100, format: 'jpeg', fallback: "/images/no_image.jpg" %>
     <br />
     <%= @user.nick_name %>
     <br />
     <%= link_to "ユーザ編集画面", edit_user_path %>
     <br />
     <%=link_to "退会", user_path(@user), method: :delete, "data-confirm" => "本当に退会しますか？", method: :delete %>

     
   </div>
    <div class="col-lg-8" style="border: 1px solid blue;">


        <h1>行ったイベント</h1>
        <div style=" overflow: scroll; height: 200px;">
        <% @user.asiatos.each do |f| %>
            <div style="border: 1px solid black">
              <%= link_to event_path(f.id) do %>
                <%= attachment_image_tag f.event, :event_image, :fill, 60, 60, format: 'jpeg', fallback: "/images/no_image.jpg" %>
              <% end %>
              <%= link_to event_path(f.event.id) do %>
                <%= f.event.event_title %>
              <% end %>

            </div>

        <% end %>
   </div>




    <h2>購入履歴</h2>

    <div style=" overflow: scroll; height: 200px;">
    <% @user.purchase_historys.each do |f| %>
        <div  style="border: 1px solid black">


            <% f.purchase_items.each do |i| %>
              <%= i.purchase_cd_title %>
            <% end %>


        </div>

    <% end %>
</div>


    </div>
  </div>
</div>
